from apps.sb.settings import MEDIA_ROOT
from apps.sb.settings import PROJECT_ROOT

file_path=MEDIA_ROOT+'upload/user/'
schema_dir = PROJECT_ROOT + 'src/schemas/xsd/'
cal_grammar_file = schema_dir + 'calendar.xsd'